# Timeline Module

Unified chronological stream mapping system events (deployments, alerts, check failures) into a queryable timeline.

## Submodules
- `events/` — Log individual timeline entries
- `query/` — Filter and retrieval scopes
- `correlations/` — Overlay trace volumes and error distributions onto deployment times
